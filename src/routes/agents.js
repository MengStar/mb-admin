import React, { Component } from 'react';
import styles from './agents.less';

export default class AgentsPage extends Component {
  state = {};

  render() {
    return (
      <div className={styles.main}>
        <p className={styles.p}>AgentsPage</p>
      </div>
    );
  }
}
